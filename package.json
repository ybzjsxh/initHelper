{
  "name": "initdevice",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "electron ./",
    "debug": "electron ./ --debug",
    "build:win32": "rm -rf out && electron-packager ./ 设备注册助手 --platform=win32 --arch=x64 --icon=src/static/Logo.ico --out=./out --app-version=0.0.1",
    "build:win32asar": "rm -rf out && electron-packager ./ 设备注册助手 --platform=win32 --arch=x64 --icon=src/static/Logo.ico --out=./out --asar.unpack='**/config/config.js' --app-version=0.0.1",
    "build:win32asar_x86": "rm -rf out && electron-packager ./ 设备注册助手 --platform=win32 --arch=ia32 --icon=src/static/Logo.ico --out=./out --asar.unpack='**/config/config.js' --app-version=0.0.1",
    "build:linux": "rm -rf out && electron-packager ./ 设备注册助手 --platform=linux --arch=x64 --icon=src/static/Logo.ico --out=./out --asar.unpack='**/config/config.js' --app-version=0.0.1"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "log4js": "^4.4.0"
  },
  "devDependencies": {
    "electron": "^5.0.6",
    "electron-packager": "^14.0.1"
  }
}
